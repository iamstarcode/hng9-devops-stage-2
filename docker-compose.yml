version: '3'
services:
  frontend:
    build: ./frontend
    image: "iamstarcode/dockerized-react"
    ports:
      - 80:3000
  backend:
    build: ./api
    image: "iamstarcode/dockerized-django"
    ports:
      - 8000:8000
