version: '3'
services:
  frontend:
    build: ./frontend
    image: "iamstarcode/dockerized-react"
    networks:
      - sub-etha
  backend:
    build: ./api
    image: "iamstarcode/dockerized-django"
    networks:
      - sub-etha
  revproxy:
    build: ./reverse-proxy
    image: "iamstarcode/reverseproxy"
    networks:
      - sub-etha
    ports:
      - "80:80"
networks:
  sub-etha:
